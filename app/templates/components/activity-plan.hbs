{{#paper-list}}
  {{#each tasks as |category|}}
    {{#paper-item class="md-1-line"}}
      <div class="md-list-item-text">
        <p>{{category.text}}</p>
      </div>
      <div class="md-secondary-container">
        {{#paper-button iconButton=true onClick=(action "selectCategory")}}
          {{paper-icon "navigate_next"}}
        {{/paper-button}}
      </div>
    {{/paper-item}}
  {{/each}}
{{/paper-list}}