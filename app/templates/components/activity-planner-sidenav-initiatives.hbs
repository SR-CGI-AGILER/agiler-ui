{{yield}}

{{#paper-list}}
    {{#paper-subheader}}
        Initiatives
        {{#paper-button iconButton=true onClick=(action 'navigateToInitiativeRoute')}}
            {{paper-icon 'edit'}}
        {{/paper-button}}
    {{/paper-subheader}}
    {{!-- {{initiatives}} --}}

    {{#each initiatives as |init|}}
        {{#paper-item onClick=(action "showUsers" init)}}
            {{init.initiativeName}}
        {{/paper-item}}
    {{/each}}

 {{paper-divider}}   
    
    
    


    
{{/paper-list}}
