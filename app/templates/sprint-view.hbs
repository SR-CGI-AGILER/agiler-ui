{{outlet}}
{{#paper-toolbar class="fix-toolbar"}}
    {{#paper-toolbar-tools}}
      <h2>Today's Sprint</h2>
    {{/paper-toolbar-tools}}
  {{/paper-toolbar}}
{{#paper-list}}

    {{#each model.payload.data.tasks as |task|}}
        {{#paper-item class="md-1-line" onClick=(action 'markComplete' task)}}
            <div class="md-list-item-text">
                <p>{{task.text}}</p>
                {{paper-divider}}
            </div>
        {{/paper-item}}
    {{/each}}
{{/paper-list}}
