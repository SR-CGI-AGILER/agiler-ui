{{outlet}}
{{#paper-toolbar class="fix-toolbar"}}
    {{#paper-toolbar-tools}}
      <h2>Today's Sprint</h2>
    {{/paper-toolbar-tools}}
  {{/paper-toolbar}}
{{#paper-list}}
    {{#each model.payload.data.tasks as |x|}}
        {{#paper-item class="md-1-line"}}
            <div class="md-list-item-text">
                <p>{{x.text}}</p>
                {{paper-divider}}
            </div>
        {{/paper-item}}
    {{/each}}
{{/paper-list}}
